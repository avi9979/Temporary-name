# מרכז הבקרה של הקהילות

כלי לניהול קבוצות הוואטסאפ, הקהילות והערוצים של **רחפניסטים להייטק** ו־**D2I**.

אתר סטטי. אין שרת, אין בסיס נתונים, אין תחזוקה שוטפת.
כל הנתונים יושבים בקובץ אחד — `data/groups.json` — והכלי קורא וכותב אליו.
**הוא לא נוגע בוואטסאפ בשום צורה אוטומטית**, ולכן אין סיכון לחסימה.

---

## שני הדפים

| דף | מה הוא עושה | מי נכנס אליו |
|---|---|---|
| `/join/?c=drones` | **הקישור הנצחי.** בודק איזו קבוצה יש בה מקום ומעביר לשם. | כל מי שרוצה להצטרף |
| `/` | **מרכז הבקרה.** כל הקבוצות, המשימות, ההודעות והניהול. | אתם |

### הקישור הנצחי

```
https://<הדומיין-שלכם>/join/?c=drones     ← רחפניסטים להייטק
https://<הדומיין-שלכם>/join/?c=d2i        ← D2I
```

זו הכתובת שמפרסמים. פעם אחת. לתמיד.
כשקבוצה מתמלאת — הכתובת נשארת אותה כתובת והיעד מאחוריה משתנה לבד.
פוסט משנה שעברה ימשיך להביא אנשים.

**סדר הניתוב:**
1. בין הקבוצות הפתוחות בתור של המשפך, לפי **אופן הניתוב** שהוגדר לו (לשונית ניהול → משפכים):
   - **ברצף** (ברירת המחדל) — ממלאים קבוצה אחת עד הסוף, ואז עוברים לבאה בתור.
   - **מאוזן (Load Balancer)** — כל כניסה הולכת לקבוצה עם אחוז התפוסה הנמוך ביותר,
     כך שכל הקבוצות במשפך גדלות יחד באותו קצב, ובכל רגע יש בהן בערך אותו מספר אנשים.
2. אם כל הקבוצות מלאות — *רשת הביטחון* של המשפך (ערוץ או קהילה, בלי הגבלת חברים).
3. אם גם היא לא מוגדרת — מסך מנומס שאומר "אנחנו פותחים קבוצה נוספת", והמשימה קופצת אצלכם במרכז הבקרה.

---

## הקמה חד־פעמית

### 1. פרסום האתר

**נטליפיי (ברירת המחדל של הריפו הזה):** מחברים את הריפו לאתר חדש ב־Netlify
(Add new site → Import from Git). אין build command ו־publish directory הוא
`.` — הכול כבר מוגדר ב־`netlify.toml`. נטליפיי גם תבנה לבד את פונקציית
המונה (`netlify/functions/counter.js`). זהו, אין שום שלב ידני נוסף.

**לחלופין, GitHub Pages:** אם מעדיפים לא להשתמש בנטליפיי —
**Settings → Pages → Source: `GitHub Actions`.**

זה הצעד הידני היחיד בהקמה, ואין דרך לעקוף אותו: יצירת אתר Pages דורשת
הרשאת admin על הריפו, והטוקן של Actions לא מקבל אותה
(`Resource not accessible by integration`).

אחרי הקליק: Actions → "Deploy to GitHub Pages" → Re-run.
מכאן והלאה כל דחיפה ל־`main` מעלה את האתר לבד, בכתובת
`https://avi9979.github.io/Temporary-name/`.

לדומיין משלכם: Settings → Pages → Custom domain, ואז הקישור הנצחי נראה כמו
`https://join.your-domain.com/?c=drones`.

### 2. טוקן שמירה
כדי שכפתור "שמור לגיטהאב" יעבוד:

1. GitHub → Settings → Developer settings → **Fine-grained personal access token**
2. Repository access: **רק הריפו הזה**
3. Permissions → Repository permissions → **Contents: Read and write**
4. העתיקו את הטוקן והדביקו בלשונית **ניהול** במרכז הבקרה.

הטוקן נשמר רק ב־`localStorage` של הדפדפן שלכם ונשלח רק ל־`api.github.com`.
אל תזינו אותו במחשב משותף. אם הוא דלף — בטלו אותו בגיטהאב וצרו חדש.

### 3. מילוי הנתונים
בלשונית **ניהול**: הדביקו את קישורי ההזמנה האמיתיים, הזינו את מספר החברים
הנוכחי בכל קבוצה, וסדרו את התור בכל משפך. ואז "שמור לגיטהאב".

---

## מספר החברים

השדה היחיד שמשתנה כל הזמן. הוא עובד בשני שלבים:

**כיול ידני** — פותחים את הקבוצה בוואטסאפ, מסתכלים במספר, מזינים.
הכלי מסמן את התאריך ומזכיר לכם לכייל מחדש אחרי מספר הימים שהגדרתם.

**מונה (אופציונלי)** — כל פעם שהקישור הנצחי מנתב מישהו לקבוצה, נרשם "hit" לאותה
קבוצה ספציפית, וזה מקדם את המספר המשוער בין כיול לכיול, כך שהמספר לא מתיישן.
מספר משוער מסומן ב־`~`. זו הערכה, לא ספירה מדויקת — לוואטסאפ אין API שמדווח מי
באמת השלים הצטרפות, רק מי נכנס לקישור.

### מונה על נטליפיי (ברירת המחדל של הריפו הזה)

האתר הזה מוגדר לרוץ על **Netlify**, וכבר כולל הכול בשביל זה: פונקציית שרת אחת
(`netlify/functions/counter.js`) ששומרת את המספרים ב־Netlify Blobs — אחסון
שמובנה בנטליפיי בלי שום הקמה, בלי חשבון חיצוני ובלי מפתחות API.

**זה כבר מוכן — אין כלום להקים.** מרגע שהריפו מחובר לאתר בנטליפיי:
1. נטליפיי מתקינה את `@netlify/blobs` לבד (מוגדר ב־`package.json`) ובונה את
   הפונקציה לבד (מוגדר ב־`netlify.toml`).
2. שדה "כתובת המונה" בלשונית ניהול כבר מצביע על `/api/counter`.
3. זהו. כל הפניה דרך `/join/?c=…` סופרת את עצמה אוטומטית.

אם רוצים לוודא שזה עובד: פתחו `https://<האתר-שלכם>/api/counter?action=all`
בדפדפן — אמור לחזור JSON כמו `{"counts":{}}` (או עם מספרים, אם כבר היו הפניות).

### מונה על אחסון אחר (Google Apps Script)

אם האתר לא רץ על נטליפיי (למשל GitHub Pages בלי שכבת שרת), אפשר להשתמש
ב־`tools/counter.gs` במקום — מונה חלופי מבוסס Google Apps Script, עם אותו
פרוטוקול בדיוק (`?action=hit&node=…` / `?action=all`), כך שאין שום שינוי קוד:
1. פתחו [script.google.com](https://script.google.com) → New project.
2. הדביקו את התוכן של `tools/counter.gs`.
3. Deploy → New deployment → **Web app**, Execute as *Me*, Who has access *Anyone*.
4. העתיקו את כתובת ה־`/exec` ושימו אותה בשדה "כתובת המונה" בלשונית ניהול
   (במקום `/api/counter`).

בלי אף מונה הכול עובד — פשוט על המספרים הידניים.

---

## המשימות

לשונית **משימות** היא לא דוח, היא רשימת פעולות. היא מתמלאת לבד כשמשהו דורש תשומת לב:
קבוצה שהתמלאה, קבוצה שכמעט מלאה ואין אחריה הבאה בתור, קישור חסר,
מספר חברים שלא כויל יותר מדי זמן, משפך בלי רשת ביטחון, או קמפיין שלא נשלח לכולם.

---

## הודעה לכולם

מנסחים פעם אחת, והכלי עוקב למי כבר שלחתם ולמי לא.
לכל יעד יש "העתק נוסח" ו"פתח בוואטסאפ" — אתם מדביקים ושולחים.

כל קמפיין שנשמר נשאר ברשימת "כל הקמפיינים השמורים" באותה לשונית — אפשר להעתיק
את הנוסח של כל קמפיין ישן בכל רגע, גם בלי להפוך אותו לפעיל.

**זה מכוון.** אין כאן שליחה אוטומטית ואין שימוש בכלי אוטומציה לוואטסאפ,
כי המחיר של חסימה הוא אובדן כל הקהילות. הכלי חוסך את ההקלדה והזיכרון, לא את השליחה.

---

## מבנה הקבצים

```
index.html                    מרכז הבקרה
join/index.html               הקישור הנצחי (הנתב)
assets/core.js                טעינת נתונים + כל חוקי הניתוב והמצב
assets/app.js                 לוגיקת מרכז הבקרה
assets/style.css              עיצוב (RTL, מצב בהיר וכהה)
data/groups.json              מקור האמת היחיד
netlify.toml                  הגדרות נטליפיי (functions + redirect ל־/api/counter)
netlify/functions/counter.js  המונה על נטליפיי (Netlify Blobs, בלי הקמה)
package.json                  תלות יחידה: @netlify/blobs, לבניית הפונקציה
tools/counter.gs              מונה חלופי לאחסון שאינו נטליפיי (Google Apps Script)
```

### `data/groups.json`

```jsonc
{
  "config":  { "warnAtPercent": 85, "staleAfterDays": 14, "counterEndpoint": "/api/counter", "repo": {…} },
  "brands":  [ { "id": "drones", "name": "רחפניסטים להייטק" } ],
  "funnels": [ { "id": "drones", "order": ["drones-1", "drones-2"], "fallbackNodeId": "drones-channel",
                 "routingMode": "sequential" } ],  // "sequential" | "balanced"
  "nodes":   [ { "id": "drones-1", "platform": "whatsapp_group", "inviteUrl": "…",
                 "capacity": 1024, "members": 1010, "calibratedAt": "2026-09-01",
                 "acceptingJoins": true } ],
  "history": [ { "date": "2026-09-01", "total": 3120 } ],
  "broadcasts": []
}
```

* **node** = קבוצה, קהילה או ערוץ בודדים.
* **funnel** = תור של nodes מאחורי קישור נצחי אחד.
* **history** = תמונות מצב יומיות. זה מה שמאפשר לענות "כמה נכנסו החודש" במספר.

אפשר לערוך את הקובץ גם ידנית בגיטהאב. הכלי יקרא ממנו בפעם הבאה שיטען.

---

## הערה על פרטיות

GitHub Pages הוא אתר ציבורי. מרכז הבקרה לא מכיל סודות (הטוקן נשאר בדפדפן),
אבל **מספרי החברים והקישורים גלויים** למי שיודע את הכתובת.
הדפים מסומנים `noindex` ולא מקושרים מאף מקום.
אם זה לא מספיק לכם — אפשר להריץ את מרכז הבקרה מקומית
(`python3 -m http.server` בתיקיית הפרויקט) ולהעלות לגיטהאב רק את `join/`.
